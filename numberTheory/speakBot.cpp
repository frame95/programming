#include <stdio.h>
#include <vector>
#define D 100 // days 
#define C 3   // colors
#define B 2   // bots

using namespace std;

struct msg {
	int text;
	int id_sender;
	int id_target;
};

class Bot {
	
	public:
		swap_lang(int i, int j) {
			int temp;
			temp = lang[i];
			lang[i]=lang[j];
			lang[j] = temp;
			return;
		}
		
		send_msg(msg T) {
			target.get_msg(msg T);
		}
		
		get_msg(msg T) {
			buff=
		}
		
	private:
		int id;
		int mybr;
		int need;
		int buff;
		int happy;
		int swap[B];
		int lang[C];	
};

int main() {
	
	
	return 0;
}

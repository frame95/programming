#include <stdio.h>
#include <stdlib.h>
#include <vector>

using namespace std;

class graph {
		int dim;
		vector< vector<int> > G;
		int vector< vector<int> > weight;
	public:
		graph(int _dim, vector< vector<int> > _G, vector< vector<int> > _weight ) {
			dim=_dim;
			G=_G;
			weight = _weight;
			}
		
		
			
	};
	
	
class plancia {
		int dim;
		
		
		
	
